displayName: Hp Auth
type: middleware

import: github.com/traefik/hpauth

summary: 'prepare hotel planner auth token and set authorization header'

testData:
  Headers:
    X-Demo: test
    X-URL: '{{URL}}'
  HpConfig:
    apiKey: "xxx"
    secretKey: "xxx"
    accountId: "xxx"